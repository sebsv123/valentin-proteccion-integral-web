import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { ProductSubpagePageView } from '@/app/seguros/[slug]/[subslug]/page'; import { subpages } from '@/lib/products'; import { getLocalizedSubpage } from '@/lib/product-locales';
const subpage = subpages.find((item) => item.parent === 'viaje' && item.slug === 'estudios')!; const english = getLocalizedSubpage(subpage, 'en'); const url = 'https://valentinproteccionintegral.com/en/insurance/travel-insurance/students'; const esUrl = 'https://valentinproteccionintegral.com/seguros/viaje/estudios';
export const metadata: Metadata = { title: english.metaTitle, description: english.metaDescription, alternates: { canonical: url, languages: { es: esUrl, en: url, 'x-default': esUrl } }, openGraph: { title: english.metaTitle, description: english.metaDescription, url, locale: 'en_GB', type: 'website' } };
export default async function EnglishTravelStudents({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (locale !== 'en') notFound(); return <ProductSubpagePageView parent="viaje" subpage={subpage} locale="en" />; }
